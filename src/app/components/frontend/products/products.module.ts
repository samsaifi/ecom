import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { ProductRoutesRoutes } from './product-routes.routing';

@NgModule({
    declarations: [],
    imports: [CommonModule, ProductRoutesRoutes],
})
export class ProductsModule {}
